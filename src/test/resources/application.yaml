spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/flat_rent_test
    password: postgres
    username: postgres

logging:
  level:
    org:
      springframework:
        jdbc:
          core:
            JdbcTemplate: DEBUG